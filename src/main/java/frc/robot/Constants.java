package frc.robot;

import frc.robot.Subsystems.MotorSubsystem;

public final class Constants {
    /*
     * TO DO:
     * Create an ID constant that the motor will use when being instantiated
     * It should be a "static final int", as this should never be changed while the program is running
     * It should also be public to allow it to be accessed by other programs
     */
    //motor IDs
    

    //operation IDs
    public static final int kControllerPort = 0;

    //subsystems
    public static final MotorSubsystem motorSubsystem = new MotorSubsystem();

}
